; -------------------------------------------------------------------------
; WORK OUT 2 PLUS 2.
; -------------------------------------------------------------------------

;	CLO		; Close unwanted windows.

	MOV	AL,02	; Copy a 2 into the AL register.
	MOV	BL,02	; Copy a 2 into the BL register.
	ADD	AL,BL	; Add AL to BL. Answer goes into AL.

	END		; Program ends.
	
; -------------------------------------------------------------------------
;
; YOUR TASKS
;
; 1)  Write a program that subtracts using SUB.
;
; 2)  Write a program that multiplies using MUL.
;
; 3)  Write a program that divides using DIV.
; 4)  Write a program that divides by zero. Make a note to avoid
;    doing this in real life.
;
; -------------------------------------------------------------------------
